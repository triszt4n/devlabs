/**
 * registers the developer as admin and with the attributes from body,
 * fill name with username as well. then redirect to /
 */
module.exports = function (objectRepository) {

    return function (req, res, next) {
        return next();
    };
  
};