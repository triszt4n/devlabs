/**
 * edit role's attribute with given data from body
 * if no data passed from body, move on.
 */
module.exports = function (objectRepository) {

    return function (req, res, next) {
        return next();
    };
  
};