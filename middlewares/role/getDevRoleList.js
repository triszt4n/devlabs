/**
 * return the developer's (by given devID) all roles as list of objects (roleList)
 * each object contains roleID and title of sprint 
 */
module.exports = function (objectRepository) {

    return function (req, res, next) {
        return next();
    };
  
};