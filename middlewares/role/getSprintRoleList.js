/**
 * return the sprint's (by given sprintID) all roles as list of objects (roleList)
 * each object contains roleID and name of developer
 */
module.exports = function (objectRepository) {

    return function (req, res, next) {
        return next();
    };
  
};